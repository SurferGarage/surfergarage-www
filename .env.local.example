# 生产域名（sitemap / OG / JSON-LD 绝对地址）
# NEXT_PUBLIC_SITE_URL=https://www.lang-qian.com

# WebGL 分级（默认不设置 = off，避免低配机打开页面卡死）
# off  = 仅 CSS 氛围，无 Canvas
# lite = 仅 Hero 海面（低网格、无 Bloom）
# full = Hero + Bloom + 全屏水下体积 Shader
# NEXT_PUBLIC_SG_WEBGL=off

# GSAP 调试 markers
# NEXT_PUBLIC_GSAP_DEBUG=1

# 开发时 node 内存持续上涨：优先用 `npm run dev:preview`（无 HMR）；
# 或改代码后重启 dev，勿长期挂着 `next dev` 不关。
# Windows Desktop 若指向 D: 盘，请始终用 `npm run dev`（会自动 realpath），勿手动 next dev。

# 招聘投递：飞书多维表格入库（app/api/recruit）
# 多维表格需要包含以下字段（文本列即可从代码写入）：
#   姓名 / 微信 / 手机号 / 邮箱 / 意向岗位 / 自我介绍 / 简历链接
#   来源 / 渠道代码 / 归因码 / 落地页 / 投递时间
# 另新增一个「简历附件」字段（类型 = 附件）。
# 投放链接：
#   小红书   https://www.lang-qian.com/join
#   官网     https://www.lang-qian.com/join?src=site
#   微信群   https://www.lang-qian.com/join?src=wechat-group&ref=WX-GROUP-001
#   公众号   https://www.lang-qian.com/join?src=wechat-oa&ref=WX-OA-001
#   B站      https://www.lang-qian.com/join?src=bilibili&ref=BILI-001
# FEISHU_APP_ID=
# FEISHU_APP_SECRET=
# FEISHU_APP_TOKEN=
# FEISHU_TABLE_ID=
