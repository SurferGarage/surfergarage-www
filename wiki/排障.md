# 排障与发布

> **审查**：2026-05-29 · 构建通过。

## 常见报错

### `IntersectionObserver: rootMargin must be pixels or percent`

片场视频挂掉。勿用 `vh`；已用 `lib/use-founder-panel-visible.ts` 滚动测量。

### ScrollTrigger 全失效

Lenis 卸载时 `kill` 全部 ST → **刷新页面**；或刚开启「减少动态效果」。

### 滚到底背景发灰 / 竖条

`--depth-t` 非法 → 仅 `formatSafeDepthProgress` 可写；`WaterVolumeFx` 的 `smoothstep` 须 `edge0 < edge1`；`footer` 需实底。

### Hero 底黑带 / Proof 顶跳色

保持 `submerge` **118vh** + `#manifesto::after`；勿加 bridge 渐变条。

### Hydration mismatch

`suppressHydrationWarning` on `<html>`；时钟用 `hero-rec-clock`；构建日用 `NEXT_PUBLIC_BUILD_TIME`。

### 本地「没有 WebGL 浪面」

**预期**：默认 `NEXT_PUBLIC_SG_WEBGL=off`。开发要看浪：`full`（见 `.env.local.example`）。

### 触点看不到 X / YouTube

**预期**：`href: "#"` 被 `isSocialChannelLive` 过滤；数据仍在 `social-channels.ts` 待上线。

### `next build` 字体失败

构建机需访问 Google Fonts，或改自托管。

## 性能排查

1. `.env` 是否误开 `full` WebGL  
2. Glow shimmer / blur  
3. Hero 离屏是否停帧  
4. 用 `dev:preview` 代替长期 `next dev`（减内存）

## 发布前检查

- [ ] `npm run lint` && `npm run build`  
- [ ] `NEXT_PUBLIC_SITE_URL`  
- [ ] `prefers-reduced-motion` 可读  
- [ ] 片场 B 站四期可播；微信为单篇 URL  
- [ ] `/privacy`、sitemap、OG  
- [ ] 锚点：宣言 / 片场 / 触点 / 联络  

## 上线缺口

| 项 | 说明 |
|----|------|
| 专用 favicon 套件 | 现用 `brand-sg-logo.png` |
| X / YouTube | 替换 `social-channels.ts` 中 `#` |
| 微信文章链 | 换真实 `mp.weixin.qq.com/s/...` |
| 备案 / 统计 | 按需 |

## Agent

动效改动说明性能影响；以代码为准，改行为同步改 wiki。
