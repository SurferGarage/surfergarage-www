# 01. 架构与模块说明

## 顶层结构

  - `app/layout.tsx`
  - 注入全局字体变量（含 **300** 字重供光学补偿）
  - 用 `SmoothScroll` 包裹；其内 **`HeroWaveCanvas variant="global"`**（固定视口 WebGL 底床）与 **`z-[1]`** 页面根并列；内容根节点带 **`data-scroll-depth-root`**，供整页 `--depth-t` ScrollTrigger 取高
  - `metadata`：`metadataBase` 恒来自 **`getSiteUrl()`**（`lib/site-url.ts`）；`openGraph` / `twitter`（含默认图 `/brand-sg-logo.png`）、`alternates.canonical`
- `app/page.tsx`
  - 页面结构主入口（Hero / **Founders** / **片场↔触点 海沟** / **Social** / Call）
  - 挂载 `GlowRiver`、`HomeHero`、`HomeFounders`、**全宽 `data-social-founders-trench`（`SOCIAL_FOUNDERS_TRENCH_MIN_H`，与叠卡呼吸带同量级）**、`HomeSocial`、`HomeCall`、`HomeScrollChoreography`
  - **刻意不做**：全页 SVG `feTurbulence` 胶片噪点层（易呈大块伪影）；背景仅依赖 `sg-main-depth` + 既有光效栈
  - 顶栏 `nav` 锚点：`#manifesto` `#founders` `#social` `#call`（与主列 **宣言 → 片场 → 触点 → 联络** 顺序一致）；**Logo 链回 `#manifesto`**：字标仅 **Surfer / Garage**（`alt="SurferGarage logo"`），与 Manifesto 主字标 **「浪前」+ `SURFERGARAGE` 巨型英文骨牌**区分
  - **首页根容器**（`app/page.tsx`）背景：**`sg-main-depth-paper`**（常驻 editorial paper）+ **`sg-main-depth-submerge`**（首屏 **118vh** 水下色温，**`opacity: var(--ambient-mode)`** 随 `--depth-t` 淡出；**禁止**用整页 % 提前截断，见 **`wiki/04` §12**）
  - **背景栈 z 序（仅首页 `page.tsx`）**：`z-0` 双轨 depth → **`GlowRiver`**（`z-[1]`）→ **`z-[2]`** **`WaterVolumeFx`** + **`UnderwaterLightStage`**（均读 **`--ambient-mode`**）→ **`main` / `footer`** `z-[3]`；Hero **`#manifesto::after`** 底缘溢出渐变衔接 Proof

## 关键模块

### 1) 滚动与动画编排

- `components/smooth-scroll.tsx`
  - 初始化 Lenis，**`LenisContext.Provider`** 向子树暴露实例
  - 与 ScrollTrigger 同步（`scrollerProxy` 绑定 `document.documentElement`）
  - **卸载 Lenis 时**（例如用户启用系统级「减少动态效果」导致不创建 Lenis、或热更新/路由卸载）：会执行 `ScrollTrigger.getAll().forEach((t) => t.kill())`，即清除**当前页面内全部** ScrollTrigger 实例
  - `resize`：同步调用 `lenis.resize()` 与 `ScrollTrigger.refresh()`
- `components/wave-scroll-velocity-bridge.tsx`
  - 订阅 Lenis `scroll`，**`gsap.quickTo`** 阻尼速度 → `:root` **`--wave-scroll-vel`**，供 Hero Shader **`uScrollVel`** 读取
- `components/home-scroll-choreography.tsx`（Client）
  - 编排入口：`gsap.context` + `matchMedia` + `ScrollTrigger.refresh`（`resize` / `fonts.ready`）；**`register-*` 顺序与 L0–L3 语义**见 **`wiki/07-动画编排与统筹.md`**
  - **动效参数字典**：`lib/sg-motion-system.ts`（`SG_SCRUB` / `SG_LOAD` / `SG_REVEAL` / `SG_SOCIAL` / `SG_FOUNDER_PIN` / `SG_CLIP`），各 `register-*` 只引用常量，保证层次与手感一致
  - 具体注册拆至 `components/motion/`：`register-manifesto-scroll`、`register-hero-choreography`、`register-founders-intro`、**`register-founder-breath`**、`register-github-repo-cards`、`register-social-expand`、`register-call-reveal`、**`register-global-depth-scrub`**、`register-desktop-pins`；插件侧 `motion/gsap-register.ts`
  - **`ctx` 内注册顺序**（`reduced-motion` 未开启时，与代码一致）：**`register-global-depth-scrub`**（`id: global-depth-t`，`trigger: [data-scroll-depth-root]`，`scrub` 写 `--depth-t`；**`formatSafeDepthProgress`**：`Number.isFinite` 校验 + **`[0,1]` 钳制**，避免 Lenis / `scrollerProxy` 边界下 `self.progress` 为 **NaN/Infinity** 时写出非法字符串，导致 `:root` **`--sg-dt`** 与 **`html` / `.sg-main-depth` 的 `color-mix`** 整条失效）→ Manifesto 景深 → Hero → … → Founders **`data-founders-intro`** → **`register-founder-breath`（`[data-founder-breath]` 内 glow 随滚 scrub 透明度）** → … → Call；**`matchMedia (md+)`** 内：`register-desktop-pins`；`reduced-motion` 下不写 `--depth-t` 由 ST、仅置 `0`

### 2) Manifesto 首屏（Hero）

- `components/home-hero.tsx`
  - `data-hero-wave`：承载 CSS 变量 `--wave-distortion`、`--wave-opacity`（**WebGL 画布在 layout**，此处仅作控制面与 IO 观察锚点）
  - 首屏正文：无整屏渐变遮罩（避免 `oklch` 与 `transparent` 插值产生横向色带）；主内容容器用轻 **`text-shadow`** 保证字在 Shader 上的可读性
  - **12 列网格**：`data-manifesto-pin` 占 **`md:col-span-2`**（第 1–2 列）；`data-manifesto-right` 自 **`md:col-start-3`** 起（大屏可收为 `lg:col-span-8`），非对称留白
  - 主标题 `h1#hero-title`：**上「浪前」**（`--font-zh`，浅灰 `#A1A1AA`、`font-light`）+ **下 `SURFERGARAGE` 一行全大写**（`wordmark-display`：`font-weight` 700、**`-0.04em` 字距**、`clamp` 巨型字号）；**`data-hero-wordmark`** 挂在英文骨牌外层 `div`（GSAP `manifesto-wordmark` 仍缩放该块）
  - 英文说明句 **We record builders…** 使用 **`rgba(255,255,255,0.72)`**，避免品牌青小字贴渐变发糊
- `components/hero-wave-canvas.tsx`（Client）
  - **`variant="global"`**：`app/layout.tsx` 单例；**`variant="embedded"`**（可选）旧式区块内嵌
  - R3F `Canvas` + 全屏 `ShaderMaterial` + `EffectComposer` / `Bloom`
  - `IntersectionObserver` + `visibilitychange` 控制 `frameloop`（观察 `hostSelector`）
  - `(pointer: coarse)` 下减弱指针扰动；**global** 下 **`pointer-events-none`** 壳层，不做指针雕刻
  - 浪面顶点色每帧读 `:root` **`--depth-t`**，随下潜略压暗（与 `sg-main-depth` 同轨）；**无拓扑线框**，为平滑三角面 + Bloom

### 3) 全站背景系统（非首屏独占）

- `components/water-volume-fx.tsx`（Client，**仅首页 `page.tsx` 引用**）
  - 全屏正交 R3F `Canvas` + 单 `ShaderMaterial` 全屏四边形；片段着色器：**fbm 焦散**、顶缘 **丁达尔锥**、稀疏 **微粒**；每帧读 `:root` **`--depth-t`**、**`--wave-scroll-vel`**（与 Hero 浪速同源）
  - **`prefers-reduced-motion: reduce`：不挂载**（零该 Canvas）；`dpr` 上限 **1.5**、`antialias: false`；容器 **`mix-blend-screen`** 与 `.uwl-stage` 合成，须注意与 `sg-main-depth` 叠色是否过曝（调 shader alpha 或 `.uwl-*` opacity）
- `components/underwater-light-stage.tsx`（Client，**仅首页**）
  - Lenis / `scroll` 驱动 **`--uwl-orb-x` / `--uwl-orb-y`** 与光球色（青→黄）；写 **`--uwl-tyndall`** 供 CSS 丁达尔层随深度减弱；DOM：**`.uwl-surface`**、**`.uwl-aperture`**、**`.uwl-godrays`**、**`.uwl-orb-*`**（样式见 `app/globals.css`）
  - 读 `:root` **`--depth-t`** 时：光球纵轨 **`yProg`** 与 **`colorT`** 均在 **`Number.isFinite` 为真时 `clamp(0,1)`**，与 **`register-global-depth-scrub`** 写入语义一致，避免偶发越界读影响丁达尔与配色
  - **`prefers-reduced-motion: reduce`：不挂载**
- `components/glow-river.tsx`
  - SVG 渐变路径（主路径 + shimmer 路径）
  - 低频漂移/呼吸，营造“暗流”
- `components/home-call.tsx`
  - 右侧光球（orb + core 双层）
  - 呼吸与轻浮动动画
  - 首条 `data-call-item`：**Community Assistant** + `AssistantQrSlot`（`lib/community-assets.ts` 约定 `public` 路径）

### 4) 社交矩阵与创始人访谈

- `lib/site-contact.ts`
  - Discord 邀请、`hello@` / `partners@` 邮箱
- `lib/community-assets.ts`
  - 助手二维码在 `public/` 下的文件名（`ASSISTANT_WECHAT_QR_SRC`）
- `lib/founders-scroll-rhythm.ts`
  - **`FOUNDER_BREATH_MIN_H`**：`home-founders` 内相邻 `data-founder-panel` 之间 **`data-founder-breath`** 全宽呼吸带最小高度（与 `globals.css` **`.sg-founder-breath-glow`** 慢漂 + `register-founder-breath` scrub 叠用）
  - **`SOCIAL_FOUNDERS_TRENCH_MIN_H`**：等于 `FOUNDER_BREATH_MIN_H`，由 **`app/page.tsx`** 在 **`#founders` 与 `#social` 之间**插入 **`data-social-founders-trench`**（海沟与叠卡呼吸**同量级**，分隔片场与触点出站矩阵）
  - **`FOUNDERS_BRIDGE_SOCIAL_PB` / `FOUNDERS_BRIDGE_SECTION_PT`**：Connect / Founders **区块内侧**收边（`pb-10 md:pb-14`、`pt-10 md:pt-14`），与海沟分工，避免双重巨型留白
  - **`FOUNDERS_EDITORIAL_MB`**：`wechat_oa` 内编辑部与 `ModuleShell` 间距
- `lib/founder-panels.ts`
  - `FOUNDER_STACK_MODULES`：五屏全高叠卡（**Surfing Founders 人物访谈** 微信专栏 **`WeChatOfficialFeed`**、视频号占位、GitHub、`GithubRepoCard`、宣传图、联系）；`FounderStackKind`；GitHub 遥测仍走 `lib/github-repo-card.ts`
- `lib/social-channels.ts`
  - `SOCIAL_CONNECT_SECTIONS`：Connect 区按 **文章 / 视频与播客 / 开源** 分块；`SOCIAL_CHANNELS` 为其扁平聚合（无 Discord 卡；社群入口见 The Call）
  - 每条 `SocialChannel` 含 **`mark: SocialChannelMarkId`**，与 **`components/social-channel-mark.tsx`** 右上角线标一一对应（**扁平**框 + 各平台抽象 SVG，非官方商标文件）
- `components/social-channel-mark.tsx`
  - Connect 卡 **右上角**线标：`aria-hidden`；**扁平**：`border-white/10`、`bg-white/[0.02]`，无渐变发光框；随父级 **`group-hover`** 仅微抬边框/底色/图标色（与渠道卡同一套「无位移」hover）
- `components/home-social.tsx`
  - `id="social"`、`scroll-mt-[4.5rem]`；**区块纵向**：`pt-16 md:pt-20` + **`FOUNDERS_BRIDGE_SOCIAL_PB`**
  - `data-social-intro`（与 stage 同轨 scrub）；`data-social-stage`（clip-path scrub）、`data-social-card`（`y` stagger）
  - 渠道卡：**扁平粗野** — `bg-white/[0.02]`、`border-white/10`，**无**渐变面 / 外阴影 / **hover `translate-y`**；可点链仅 **`hover:bg-white/[0.05]`** + 边框 `color-mix` 闪电蓝；**标签** `font-mono` 全大写 `text-[10px]`；**标题** `#FFFFFF` `font-medium`；**说明** `#A1A1AA`
  - **`sec.id === "articles"`**：单列 **`max-w-md`** 左齐（与标题同侧），与视频/开源三列网格区分宽度
- `components/home-founders.tsx`
  - `id="founders"`；**`aria-label="浪前片场"`**；`data-founders-stack` 内五屏 `FOUNDER_STACK_MODULES`：每屏 `</article>` 与下一屏之间插入 **全宽 `data-founder-breath`**（**不在** `data-founder-card` 内，避免被桌面 pin/scrub 误伤），高度类来自 **`FOUNDER_BREATH_MIN_H`**
  - 每屏：`data-founder-panel` + `data-founder-module`（kind）+ **`data-founder-card`**：共用 **`FOUNDER_CARD_BASE`**（桌面 **`min-h-[100dvh]`** + **`justify-center`**）；**`wechat_oa`**：`data-founders-intro`（编辑部）在 **`ModuleShell`（Surfing Founders 人物访谈）** 之上；纵向留白由 **`FOUNDERS_BRIDGE_SECTION_PT`、`FOUNDERS_EDITORIAL_MB`** 与 **`ModuleShell`↔列表 `gap-16 md:gap-24`** 统筹；**`WeChatOfficialFeed`** 为**与主栏同宽的横滑列表**（负 margin 对齐 `px-5/md:px-10/lg:px-12`，无 `w-screen` breakout）；GitHub 为第 3 屏；公众号叠卡纵向 pin 见 `register-desktop-pins` / **`FOUNDER_WECHAT_PIN_END`**；数据 `lib/founder-panels.ts`
- `components/wechat-official-feed.tsx`（Client）
  - **Surfing Founders 人物访谈 · 微信专栏**：**`overflow-x-auto`** 横滑；**`CardContainer` / `CardBody` / `CardItem`** 提供 **仅 hover 区** 的 **指针 3D 倾角**（`maxTilt` 8、`prefers-reduced-motion` 下为 0），**无** 全局 RAF、**无** ScrollTrigger 写 `scrollLeft`；玻璃渐变壳 + 封面暗角与 hover 光晕；**`data-wechat-official-feed`**、**`data-wechat-card`**；**`app/globals.css`** **`.wechat-official-feed-scroll`**
  - 数据 **`lib/wechat-official-feed.ts`**：每项 **`titleZh`**、`href`、`imageSrc`；图 **`public/wechat-feed/01.png`…`08.png`**
- `lib/founder-wechat-pin-end.ts`
  - **`FOUNDER_WECHAT_PIN_END`**：须与 `register-desktop-pins` 中公众号叠卡 **`wechat_oa`** 的 **`end`** 一致（纵向 pin 时长）。**`WECHAT_PIN_SCROLL_INTRO_HOLD`**、**`mapWechatPinProgressToHorizontalScrub`** 等横滑映射**当前未被组件引用**，仅作历史常量保留；若未来恢复「竖滚驱动横滑」须再对齐三处
- `components/github-repo-card.tsx`
  - 文章卡可选内嵌：`data-github-repo-card`；`showTelemetry` 为真且传入 `telemetry` 时渲染 Stars/Forks 等；否则仅 `sr-only` 说明（无视觉占位）
  - `GitHubRepoCardConfig` / `GitHubRepoTelemetry`、`githubRepoUrl()`
- `components/founder-article-actions.tsx`（Client）
  - 文章卡：外链 +「复制链接」
- `components/founder-lazy-bilibili.tsx`（Client）
  - B 站 iframe：视口内挂载、离开卸载；`visibilitychange` 与离屏前调用 `lib/bilibili-embed.ts` 尝试暂停
- `lib/bilibili-embed.ts`
  - `pauseBilibiliEmbed`：`postMessage` → `player.bilibili.com`（非官方保证，失败则依赖卸载）
- `components/assistant-qr-slot.tsx`（Client）
  - The Call 二维码位；缺图占位

## 依赖关系（简化）

`layout -> SmoothScroll -> (HeroWaveCanvas global + z-[1] children[data-scroll-depth-root])`  
`page -> sg-main-depth + GlowRiver + (WaterVolumeFx + UnderwaterLightStage) + HomeHero + HomeFounders + [data-social-founders-trench] + HomeSocial + … + HomeScrollChoreography`

## 响应式断点约定

- **数值单源**：`lib/sg-breakpoints.ts` 的 **`SG_BP`**（`sm` 640、`md` 768、`lg` 1024、`xl` 1280、`2xl` 1536）与 **Tailwind v4 默认 `screens`** 一致；若在 `app/globals.css` 的 **`@theme`** 中覆盖 `--breakpoint-*`，须同步更新 **`SG_BP`** 与本节。
- **`gsap.matchMedia` / 条件 ScrollTrigger**：与 **`md:`** 对齐时使用 **`SG_MEDIA_MD_MIN`**，避免手写 `(min-width: 768px)` 等与工具类漂移。
- **`next/image` `sizes`**：优先用 **`sgMediaMaxWidth(SG_BP.*)`**（或 `sgMediaMinWidth`）拼媒询字符串，与上表对齐。
- **Turbopack workspace root**：`next.config.ts` 已设 **`turbopack.root`** 为本仓库 **`surfergarage-www`** 目录的绝对路径，消除「上级目录另有 lockfile 时被误判为 monorepo 根」的构建警告；排障见 **`wiki/04`**。

## 近期代码审查备忘（工程一致性）

- **`SmoothScroll` teardown**：仍执行 `ScrollTrigger.getAll().forEach(kill)`；全站 ST 须由 `HomeScrollChoreography`（或同级整页编排）独占创建，见下文「Lenis 与 ScrollTrigger 生命周期」。
- **`--depth-t`**：写入路径仅 **`formatSafeDepthProgress`**（`motion/register-global-depth-scrub.ts`）；滚底背景异常见 **`wiki/04` §9**。
- **公众号叠卡**：**`FOUNDER_WECHAT_PIN_END`** 须与 **`register-desktop-pins`** 中 `wechat_oa` 的 **`end`** 一致（纵向 pin 时长）。**`WeChatOfficialFeed`** 不再注册 **`wechat-feed-scrub-sync`**，横滑为纯手控。
- **海沟与呼吸带**：`SOCIAL_FOUNDERS_TRENCH_MIN_H === FOUNDER_BREATH_MIN_H`；改其一须评估另一处视觉是否仍对齐。

## Lenis 与 ScrollTrigger 生命周期（约定）

- **当前假设**：全站 ScrollTrigger 仅由 `HomeScrollChoreography`（及未来同级的「整页编排」模块）创建，并与 Lenis **同生共死**。`SmoothScroll` 在 teardown 时 `kill` 全部 ST 不会误伤「仍应存活」的第三方实例——因为目前没有第三方。
- **若未来引入**：另一套 ST（例如 CMS 注入的动画库、第二页面根布局），必须在 Lenis 关闭或路由切换时验证是否需将 `getAll().kill()` 改为 **按 `ScrollTrigger.getById` / 自定义命名空间 / 仅 `ctx.revert()`** 等更窄的作用域，避免静默清空他人实例。
- **`ScrollTrigger.refresh()` 重复**：`SmoothScroll` 与 `HomeScrollChoreography`（防抖 + `fonts.ready`）都会在布局变化时 refresh；属于**有意冗余**，优先正确性；若合并为单一入口，须保留「字体加载完成后再 refresh」这一条路径。

## RAF 与 GPU（首屏 + 滚动）

- Lenis 使用 **GSAP ticker** 驱动 `lenis.raf`；首屏 WebGL 在可见时使用 R3F **独立 RAF**。二者叠加在低端机上为预期成本，调优顺序见 `03-性能与自动降级策略.md`。

## 维护规则

- 所有 GSAP 动画必须放在 `gsap.context()` 内，确保可回收
- Pin 逻辑使用 `matchMedia` 按断点隔离
- 背景特效必须优先保证流畅性与可读性
- 新增 ScrollTrigger 前先读 **「Lenis 与 ScrollTrigger 生命周期」** 一节，避免与 `SmoothScroll` 的全局 `kill` 语义冲突

## 附录：首页区块与文件映射

| 区块 `id` | 组件 / 位置 | 备注 |
|-----------|----------------|------|
| `manifesto` | `components/home-hero.tsx` | 影院 Hero：字标 stagger、`data-hero-scrub`、REC ticker；ST `manifesto-wordmark` |
| `proof` | `components/home-proof.tsx` | **出水线**过渡：stat strip + 嘉宾 chip；背景透明，靠 `sg-main-depth` |
| `founders` | `components/home-founders.tsx` | **四屏**叠卡 pin：`wechat_column` / `video_studio` / `github_repo` / `brand_visual`；`h-[calc(100dvh-4.5rem)]` |
| （无 `id`） | `app/page.tsx` 海沟 | **`data-social-founders-trench`** |
| `social` | `components/home-social.tsx` | Bento 渠道卡 + GitHub hero；ST `social-expand` |
| `call` | `components/home-call.tsx` | 大字 CTA + 小浪 QR；ST `call-reveal` |
| `fit` | `components/home-call.tsx` → **`HomeFit`** | 匹配度双列；桌面 ST **`fit-closing-pin`**，移动 **`fit-reveal`** |
| （footer） | `components/site-footer.tsx` | Newsletter、导航列、**`data-footer-wordmark-letters`** scroll reveal |
| 全局 | `components/site-header.tsx` | 居中 logo、instant 锚点跳转、`useActiveSection` |
| 全局 | `components/sg-ambient-grid.tsx` | 点阵 + grain（`--surface-mode` 控制） |

## 附录：SEO 与站点根 URL

- `lib/site-url.ts`：`getSiteUrl()`（读 `NEXT_PUBLIC_SITE_URL`，无则默认 `https://www.surfergarage.com`；供 `sitemap` / `robots` 等绝对地址）
- `app/sitemap.ts`：首页 `/` 条目
- `app/robots.ts`：`allow: /` + `sitemap` 指向 `${getSiteUrl()}/sitemap.xml`
