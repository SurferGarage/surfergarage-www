# 01. 架构与模块说明

## 顶层结构

- `app/layout.tsx`
  - 注入全局字体变量（含 **300** 字重供光学补偿）
  - 用 `SmoothScroll` 包裹；其内 **`HeroWaveCanvas variant="global"`**（固定视口 WebGL 底床）与 **`z-[1]`** 页面根并列
  - `metadata`：`metadataBase` 恒来自 **`getSiteUrl()`**（`lib/site-url.ts`）；`openGraph` / `twitter`（含默认图 `/brand-sg-logo.png`）、`alternates.canonical`
- `app/page.tsx`
  - 页面结构主入口（Hero / Proof / Social / Founders / Call）
  - 挂载 `GlowRiver`、`HomeHero`、`HomeSocial`、`HomeFounders`、`HomeCall`、`HomeScrollChoreography`
  - 顶栏 `nav` 锚点：`#manifesto` `#proof` `#social` `#founders` `#call`；`<main>` 使用 `sg-main-depth` 纵向深度渐变（见 `app/globals.css`）

## 关键模块

### 1) 滚动与动画编排

- `components/smooth-scroll.tsx`
  - 初始化 Lenis，**`LenisContext.Provider`** 向子树暴露实例
  - 与 ScrollTrigger 同步（`scrollerProxy` 绑定 `document.documentElement`）
  - **卸载 Lenis 时**（例如用户启用系统级「减少动态效果」导致不创建 Lenis、或热更新/路由卸载）：会执行 `ScrollTrigger.getAll().forEach((t) => t.kill())`，即清除**当前页面内全部** ScrollTrigger 实例
  - `resize`：同步调用 `lenis.resize()` 与 `ScrollTrigger.refresh()`
- `components/wave-scroll-velocity-bridge.tsx`
  - 订阅 Lenis `scroll`，**`gsap.quickTo`** 阻尼速度 → `:root` **`--wave-scroll-vel`**，供 Hero Shader **`uScrollVel`** 读取
- `components/home-scroll-choreography.tsx`（Client）
  - 编排入口：`gsap.context` + `matchMedia` + `ScrollTrigger.refresh`（`resize` / `fonts.ready`）
  - 具体注册拆至 `components/motion/`：`register-manifesto-scroll`、`register-hero-choreography`、`register-proof-streams`、`register-founders-intro`、`register-github-repo-cards`、`register-social-expand`、`register-call-reveal`、`register-desktop-pins`；插件侧 `motion/gsap-register.ts`
  - **`ctx` 内注册顺序**（`reduced-motion` 未开启时，与代码一致）：Manifesto 景深 → Hero（含 `manifesto-scrub` / wave / marquee）→ Proof 编排 → Founders 区头 reveal → GitHub 仓库卡 reveal → Social 展开 → Call 时间轴；**`matchMedia (md+)`** 内单独：`register-desktop-pins`（Manifesto pin、Proof pin、Founder 叠卡 pin）

### 2) Manifesto 首屏（Hero）

- `components/home-hero.tsx`
  - `data-hero-wave`：承载 CSS 变量 `--wave-distortion`、`--wave-opacity`（**WebGL 画布在 layout**，此处仅作控制面与 IO 观察锚点）
  - 底部 `data-hero-marquee`：全宽跑马灯（样式见 `app/globals.css` 中 `.sg-marquee-*`）
- `components/hero-wave-canvas.tsx`（Client）
  - **`variant="global"`**：`app/layout.tsx` 单例；**`variant="embedded"`**（可选）旧式区块内嵌
  - R3F `Canvas` + 全屏 `ShaderMaterial` + `EffectComposer` / `Bloom`
  - `IntersectionObserver` + `visibilitychange` 控制 `frameloop`（观察 `hostSelector`）
  - `(pointer: coarse)` 下减弱指针扰动；**global** 下 **`pointer-events-none`** 壳层，不做指针雕刻

### 3) 全站背景系统（非首屏独占）

- `components/glow-river.tsx`
  - SVG 渐变路径（主路径 + shimmer 路径）
  - 低频漂移/呼吸，营造“暗流”
- `components/home-call.tsx`
  - 右侧光球（orb + core 双层）
  - 呼吸与轻浮动动画
  - 首条 `data-call-item`：**Community Assistant** + `AssistantQrSlot`（`lib/community-assets.ts` 约定 `public` 路径）

### 4) 社交矩阵与创始人访谈

- `lib/site-contact.ts`
  - Discord 邀请、`hello@` / `partners@` 邮箱
- `lib/community-assets.ts`
  - 助手二维码在 `public/` 下的文件名（`ASSISTANT_WECHAT_QR_SRC`）
- `lib/founder-panels.ts`
  - 历史叠卡类型与 **`FOUNDER_PANELS`**（当前可为空数组）；新内容矩阵见 **`lib/founder-media-modules.ts`**
- `lib/founder-media-modules.ts`
  - **`FOUNDER_MEDIA_GITHUB_REPO`**、**`FOUNDER_MEDIA_GITHUB_PAGE_URL`**（GitHub 仓库板块）
- `lib/social-channels.ts`
  - `SOCIAL_CHANNELS`：`kind` 含 `article` \| `video` \| `audio`（小宇宙）\| `community`（Discord）\| `code`；`href` 非 `http(s)` 时在 `home-social` 中渲染为静态卡（避免 `href="#"`）
- `components/home-social.tsx`
  - `id="social"`、`scroll-mt-[4.5rem]`；12 列栅格；`data-social-intro`（与 stage 同轨 scrub）；`data-social-stage`（clip-path scrub）、`data-social-card`（`y` stagger）；外链 `rel` / `target` 规则同前
- `components/home-founders.tsx`
  - `id="founders"` 为**整区容器**；子区块锚点：`#founders-wechat`、`#founders-video`、`#founders-github`、`#founders-promo`、`#founders-contact`；`data-founders-intro`、`data-founders-modules`；`GithubRepoCard` 仅在 **GitHub** 子区；**已移除** `data-founders-stack` / `data-founder-panel` / `data-founder-card`（桌面叠卡 pin 无目标元素时不创建 `founder-stack-*`）
- `components/github-repo-card.tsx`
  - 文章卡可选内嵌：`data-github-repo-card`；`showTelemetry` 为真且传入 `telemetry` 时渲染 Stars/Forks 等；否则仅 `sr-only` 说明（无视觉占位）
- `lib/github-repo-card.ts`
  - `GitHubRepoCardConfig` / `GitHubRepoTelemetry`、`githubRepoUrl()`
- `components/founder-article-actions.tsx`（Client）
  - 文章卡：外链 +「复制链接」
- `components/founder-lazy-bilibili.tsx`（Client）
  - B 站 iframe：视口内挂载、离开卸载；`visibilitychange` 与离屏前调用 `lib/bilibili-embed.ts` 尝试暂停
- `lib/bilibili-embed.ts`
  - `pauseBilibiliEmbed`：`postMessage` → `player.bilibili.com`（非官方保证，失败则依赖卸载）
- `components/assistant-qr-slot.tsx`（Client）
  - The Call 二维码位；缺图占位

## 依赖关系（简化）

`layout -> SmoothScroll -> (HeroWaveCanvas global + z-[1] children)`  
`page -> GlowRiver + HomeHero + Proof + HomeSocial + HomeFounders + HomeCall + HomeScrollChoreography`

## Lenis 与 ScrollTrigger 生命周期（约定）

- **当前假设**：全站 ScrollTrigger 仅由 `HomeScrollChoreography`（及未来同级的「整页编排」模块）创建，并与 Lenis **同生共死**。`SmoothScroll` 在 teardown 时 `kill` 全部 ST 不会误伤「仍应存活」的第三方实例——因为目前没有第三方。
- **若未来引入**：另一套 ST（例如 CMS 注入的动画库、第二页面根布局），必须在 Lenis 关闭或路由切换时验证是否需将 `getAll().kill()` 改为 **按 `ScrollTrigger.getById` / 自定义命名空间 / 仅 `ctx.revert()`** 等更窄的作用域，避免静默清空他人实例。
- **`ScrollTrigger.refresh()` 重复**：`SmoothScroll` 与 `HomeScrollChoreography`（防抖 + `fonts.ready`）都会在布局变化时 refresh；属于**有意冗余**，优先正确性；若合并为单一入口，须保留「字体加载完成后再 refresh」这一条路径。

## RAF 与 GPU（首屏 + 滚动）

- Lenis 使用 **GSAP ticker** 驱动 `lenis.raf`；首屏 WebGL 在可见时使用 R3F **独立 RAF**。二者叠加在低端机上为预期成本，调优顺序见 `03-性能与自动降级策略.md`。

## 维护规则

- 所有 GSAP 动画必须放在 `gsap.context()` 内，确保可回收
- Pin 逻辑使用 `matchMedia` 按断点隔离
- 背景特效必须优先保证流畅性与可读性
- 新增 ScrollTrigger 前先读 **「Lenis 与 ScrollTrigger 生命周期」** 一节，避免与 `SmoothScroll` 的全局 `kill` 语义冲突

## 附录：首页区块与文件映射

| 区块 `id` | 组件 / 位置 | 备注 |
|-----------|----------------|------|
| `manifesto` | `components/home-hero.tsx` | `data-hero-wordmark`；ST `manifesto-wordmark` / `manifesto-fade-depth`；首屏 load 仍用 `data-hero-reveal` |
| `proof` | `app/page.tsx` + `lib/proof-streams.ts`（`PROOF_STREAMS`） | `data-proof-intro`；`data-proof-pin` / `data-proof-streams` / `data-proof-stream`；ST `proof-choreography` |
| `social` | `components/home-social.tsx` | `data-social-intro`；`data-social-stage` / `data-social-card`；ST `social-expand` |
| `founders` | `components/home-founders.tsx` | 区头 `data-founders-intro`；子区 `#founders-wechat` … `#founders-contact`；`GithubRepoCard` 在 `#founders-github`；ST `founders-intro`、`github-repo-card-*`（无 `founder-stack-*` 时叠卡 pin 不注册） |
| `call` | `components/home-call.tsx` | `data-call-intro`；CTA；`AssistantQrSlot`；`data-call-item`；ST `call-reveal`；邮箱与 Discord 见 `lib/site-contact.ts`、`lib/community-assets.ts` |

## 附录：SEO 与站点根 URL

- `lib/site-url.ts`：`getSiteUrl()`（读 `NEXT_PUBLIC_SITE_URL`，无则默认 `https://www.surfergarage.com`；供 `sitemap` / `robots` 等绝对地址）
- `app/sitemap.ts`：首页 `/` 条目
- `app/robots.ts`：`allow: /` + `sitemap` 指向 `${getSiteUrl()}/sitemap.xml`
